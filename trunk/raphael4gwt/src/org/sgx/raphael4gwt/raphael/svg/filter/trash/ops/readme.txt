filter operations - java helpers for documenting and easy use filter operations as POJOS (not native stuff). 

the same but using only overlay over svg-filters raphael extension is done in params. 

FilterOperation is the javascript overlay type for the filter, while FilterOp is a pure java filter operaation definition
that can be used to create a  real FilterOperation using FilterOp::toJsParams()